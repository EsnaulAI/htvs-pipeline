name: gnina
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  # gnina no está en canales conda-forge/bioconda en este entorno;
  # el rescoring se ejecuta vía contenedor (gnina_container_wrapper.sh).
  - smina
